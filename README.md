# TideQuant 潮汐量化
顺势而生，逆势有度

## 安装
在仓库根目录下使用pip, 即可完成最简洁的安装:

```bash
pip install .
```

若需要包含机器学习相关依赖, 请使用extra安装:

```bash
pip install ".[ml]"
```

若需要包含深度学习相关依赖, 请使用extra安装:

```bash
pip install ".[dl]"
```
